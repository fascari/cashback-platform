[tools]
go = "1.26"

[tasks.build]
run = "go build -o ../../bin/cashback-service-api ./cmd/api/main.go"

[tasks.run]
run = "go run ./cmd/api/main.go"

[tasks.test]
run = "go test -v -race ./..."

[tasks.lint]
run = "golangci-lint run"

[tasks.deps]
run = "go mod tidy"
